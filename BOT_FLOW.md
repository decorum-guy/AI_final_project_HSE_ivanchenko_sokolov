# Визуальная логика Telegram-бота

Этот файл описывает пользовательскую навигацию бота по фактическим callback-кнопкам из `app/bot/handlers/` и `app/bot/keyboards/`.

## Старт

`/start` открывает главное меню.

Главное меню:

```text
📰 Получить дайджест
🎯 Интересы
📡 Источники
⭐ Подписки
🕒 Расписание
📚 История
⚙️ Настройки
❓ Помощь
```

## 📰 Получить дайджест

Экран выбора основы:

```text
🎯 По интересам
📡 По источникам
← Назад
```

`🎯 По интересам`:

1. Проверяет, указаны ли интересы.
2. Проверяет, выбраны ли источники.
3. Если интересов нет, открывает экран интересов.
4. Если источников нет, предлагает ИИ-подбор или ручной выбор.
5. Если все есть, открывает выбор периода.

Важно: интересы не заменяют источники. В обоих режимах бот работает с уже выбранными RSS-лентами; режим `🎯 По интересам` дополнительно сортирует новости по сохраненным темам пользователя.

`📡 По источникам`:

1. Проверяет, выбраны ли источники.
2. Если нет, открывает ручной выбор источников.
3. Если есть, открывает выбор периода.

Выбор периода:

```text
Сегодня
3 дня
Неделя
← Назад
```

После выбора периода бот:

1. показывает loading-сообщение;
2. собирает RSS;
3. обновляет progress;
4. формирует дайджест через выбранного ИИ-провайдера;
5. сохраняет дайджест в историю;
6. показывает дайджест или fallback-экран длинного сообщения.

Кнопки под обычным дайджестом:

```text
🔄 Обновить (2/1/0)
⭐ В избранное / ⭐ Убрать
👍 Полезно
👎 Не подходит
🏠 В главное меню
```

Если дайджест слишком длинный:

```text
🔁 Перегенерировать короче
📄 Получить DOCX
📕 Получить PDF
```

## 🎯 Интересы

Экран интересов показывает текущий текст интересов и кнопки:

```text
✏️ Изменить интересы
🧩 Изменить слова
🤖 Подобрать источники
📡 Выбрать вручную
← Назад
```

`✏️ Изменить интересы` открывает выбор:

```text
✏️ Свободный текст + ИИ
🧩 Слова вручную
← Назад
```

`✏️ Свободный текст + ИИ` переводит пользователя в режим ввода одного текстового сообщения. После ввода бот:

1. сохраняет исходный текст интересов;
2. просит ИИ нормализовать интересы в ключевые слова;
3. показывает две строки: что пользователь ввел и какие слова получились;
4. предлагает изменить слова вручную, подобрать источники или выбрать источники самому.

После сохранения интересов:

```text
🧩 Изменить слова
🤖 Подобрать источники
📡 Выбрать вручную
← Назад
```

`🧩 Слова вручную` открывает список нормализованных слов:

```text
➕ Добавить слова
➖ Удалить слово
← Назад
```

При добавлении бот просит писать слова в начальной форме: `игра`, `театр`, `маркетинг`. При удалении показывает каждое слово отдельной кнопкой.

`🤖 Подобрать источники`:

1. читает активные источники из `sources.xlsx`;
2. отправляет модели нормализованные слова интересов и список источников;
3. ожидает JSON с `source_id` и причиной рекомендации;
4. если модель недоступна или JSON невалиден, использует fallback по ключевым словам.

Экран рекомендаций:

```text
✅ Добавить все
⚙️ Изменить подборку
📡 Выбрать вручную
🔄 Подобрать заново
← Назад
```

`⚙️ Изменить подборку` открывает список рекомендованных источников с переключателями:

```text
✅ Источник выбран
☐ Источник исключен
💾 Сохранить
← Назад
```

Если пользователь исключил источник из ИИ-рекомендаций, номер, название и строка `Почему подходит` зачеркиваются. Пометка `(Удалено пользователем)` остается рядом с названием и не зачеркивается.

## 📡 Источники

Экран категорий строится динамически по активным источникам из `sources.xlsx`.

Формат кнопки категории:

```text
Категория (выбрано/всего)
```

Пример:

```text
IT (2/6)
Бизнес (0/4)
🧹 Очистить все
← Назад
```

`🧹 Очистить все` открывает подтверждение:

```text
✅ Да, очистить
← Назад
```

После подтверждения бот снимает все выбранные источники пользователя и возвращает на экран категорий. RSS-источники из проекта не удаляются, `sources.xlsx` не меняется.

После выбора категории открывается список источников:

```text
✅ Хабр
☐ CNews
☐ 3DNews
✅ Готово
← Назад
```

`✅` означает выбранный источник, `☐` — невыбранный. Нажатие на источник переключает состояние без нового сообщения.

Кнопка `Готово`:

- из главного меню возвращает в меню;
- из подписок возвращает в подписки;
- из сценария дайджеста продолжает к выбору периода;
- из админских тестов продолжает соответствующий тестовый сценарий.

## ⭐ Подписки

Показывает выбранные пользователем источники.

Если подписок нет:

```text
📡 Выбрать источники
← Назад
```

Если подписки есть:

```text
📡 Изменить источники
🧹 Очистить все
📰 Получить дайджест
← Назад
```

`🧹 Очистить все` открывает подтверждение:

```text
✅ Да, очистить
← Назад
```

После подтверждения бот снимает все выбранные источники пользователя и обновляет экран подписок. Интересы, история дайджестов и список RSS-источников не меняются.

## 🕒 Расписание

Главный экран:

```text
Когда присылать дайджест?
Текущая настройка: ...

🌅 Каждое утро
🌙 Каждый вечер
📅 Раз в неделю
🔕 Отключить
← Назад
```

Утро:

```text
08:00
09:00
10:00
← Назад
```

Вечер:

```text
18:00
19:00
20:00
← Назад
```

Раз в неделю:

1. выбор дня недели;
2. выбор времени;
3. если timezone еще не выбран, выбор города.

Timezone выбирается через список городов с UTC-смещениями. После первого выбора повторно не спрашивается, но его можно изменить в настройках.

## 📚 История

История показывает до 5 дайджестов на страницу.

Кнопки:

```text
22-05-2026 ⭐ — Короткое имя
21-05-2026 — Короткое имя
← →
← Назад
```

Открытие дайджеста из истории редактирует текущее сообщение.

Кнопки внутри старого дайджеста:

```text
⭐ В избранное / ⭐ Убрать из избранного
← К истории
```

## ⚙️ Настройки

Экран показывает:

```text
Текущий часовой пояс
Звук уведомлений
ИИ-провайдер
```

Кнопки:

```text
🌍 Изменить часовой пояс
🔕 Отключить звук / 🔔 Включить звук
🤖 ИИ-провайдер: GigaChat / ChatGPT
← Назад
```

Пользователь выбирает только провайдера. Конкретная модель ChatGPT задается через `.env`.

## ❓ Помощь

Показывает краткое объяснение основных режимов, разницу между ИИ-подбором источников и дайджестом по интересам, а также предупреждение о том, что ИИ может ошибаться.

## /admin

Доступно только пользователю с `ADMIN_ID`.

Админ-панель:

```text
📊 Статистика
🔍 Проверить RSS
♻️ Перезагрузить источники
🧪 Тестовый дайджест
📦 Тест длинного дайджеста
🧹 Очистить истории
← Назад
```

`📊 Статистика`:

```text
📈 Общая
👥 Пользователи
← Назад
```

`👥 Пользователи` открывает список пользователей по 8 на страницу и карточку выбранного пользователя.

`🔍 Проверить RSS` проверяет активные источники.

`♻️ Перезагрузить источники` заново читает `sources.xlsx`, не перезаписывая файл.

`🧪 Тестовый дайджест` повторяет обычный сценарий дайджеста в админском контексте.

`📦 Тест длинного дайджеста` принудительно показывает fallback-экран длинного дайджеста для проверки DOCX/PDF и укорачивания.

`🧹 Очистить истории` удаляет только историю дайджестов, избранное и оценки к дайджестам. Интересы, подписки, расписание и настройки пользователей не очищаются.
